/**
 * Returns some value.
 *
 * @since  unreleased
 * @param  string someParam Some param.
 * @return string           Some return value.
 */
function someFunction(someParam: string): string {
	return someParam
}

/**
 * Returns some other value.
 *
 * @since  unreleased
 * @param  string someParam Some param.
 * @return string           Some return value.
 */
function someOtherFunction(someParam: string): string {
	return someParam
}

/**
 * Returns a value.
 *
 * @since  1.0.0
 * @param  string someParam Some param.
 * @return string           Some value.
 */
function somePreviousFunction(someParam: string): string {
	return someParam
}
